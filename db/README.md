# Database Schema

`schema.sql` is the initial MVP baseline to support Sprint 1 stories.

Current coverage:
- organizations
- teams
- planning_cycles
- capacity_plans

Next story updates should add remaining entities from `openapi-mvp-v1.yaml`.
